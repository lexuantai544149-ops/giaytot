<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_background">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/detail_toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/surface"
            app:title="Chi tiết sản phẩm" />

    </com.google.android.material.appbar.AppBarLayout>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <ProgressBar
                android:id="@+id/productProgressBar"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:visibility="gone" />

            <ImageView
                android:id="@+id/productImage"
                android:layout_width="match_parent"
                android:layout_height="300dp"
                android:scaleType="centerCrop"
                tools:src="@drawable/shoe_placeholder" />

            <TextView
                android:id="@+id/productName"
                style="@style/DetailHeader"
                android:textSize="24sp"
                tools:text="Tên sản phẩm" />

            <TextView
                android:id="@+id/productPrice"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:textColor="@color/accent"
                android:textSize="22sp"
                android:textStyle="bold"
                tools:text="Giá tiền" />

            <Button
                android:id="@+id/btnAddToCart"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="Thêm vào giỏ hàng"
                app:backgroundTint="@color/accent" />

            <TextView
                android:id="@+id/productDescription"
                style="@style/DetailItem"
                android:layout_marginTop="16dp"
                tools:text="Mô tả sản phẩm..." />

            <!-- Các thông tin chi tiết khác -->
            <TextView style="@style/DetailHeader" android:text="Thông số kỹ thuật" />
            <TextView android:id="@+id/productBrand" style="@style/DetailItem" tools:text="Thương hiệu: Apple" />
            <TextView android:id="@+id/productCategory" style="@style/DetailItem" tools:text="Danh mục: Phụ kiện" />
            <TextView android:id="@+id/productStock" style="@style/DetailItem" tools:text="Trong kho: 50" />
            <TextView android:id="@+id/productRating" style="@style/DetailItem" tools:text="Đánh giá: 4.5 / 5" />
            <TextView android:id="@+id/productDimensions" style="@style/DetailItem" tools:text="Kích thước: 10 x 20 x 5 cm" />

            <TextView style="@style/DetailHeader" android:text="Chính sách" />
            <TextView android:id="@+id/productWarranty" style="@style/DetailItem" tools:text="Bảo hành: 12 tháng" />
            <TextView android:id="@+id/productShipping" style="@style/DetailItem" tools:text="Giao hàng: 2-3 ngày" />
            <TextView android:id="@+id/productReturn" style="@style/DetailItem" tools:text="Đổi trả: 7 ngày" />

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>